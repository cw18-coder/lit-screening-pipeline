export function Wiki() {
  return (
    <div className="route-view">
      <h1>.github/ wiki</h1>
      <p style={{ color: 'var(--ink-dim)' }}>
        Browsable rendering of the skills, instructions, and agents that govern the workspace. Coming in M4.
      </p>
    </div>
  );
}
